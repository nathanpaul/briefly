"""Clients for the external services: the Whisper transcription cluster and the
pyannote diarization service. Stdlib-only HTTP (no third-party deps); the POST
function is injectable so tests run without network."""
