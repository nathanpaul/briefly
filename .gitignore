# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
venv/
uv.lock

# Pipeline working data (per-meeting; never source-controlled)
recordings/
processed/
transcripts/

# Local-only project content (kept on disk, not published).
# CLAUDE.md/PLAN.md are anchored to the repo root so vault-template/.claude/CLAUDE.md still ships.
docs/
knowledge/
scripts/
/CLAUDE.md
/PLAN.md

# Local scratch / secrets (.env.example is the committed template)
*.part
.DS_Store
.env

# Machine-local plugin memory (MemSearch)
.memsearch/
